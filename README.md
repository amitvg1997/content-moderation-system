# content-moderation-system
This content moderation system is build using AWS resources. User can upload an image and input text on a webpage, that image and text would be moderated using aws comprehend and aws rekognition services and approved or disapproved. Only the approved content is stored in the database. The implementation is done using Python and AWS IAC.
